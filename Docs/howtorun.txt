To run the server first as a springboot application (on the folder /Package/tp2Srsc/ correr o seguinte comando "mvn spring-boot:run") and then the client (Simple main file in client folder)

Note: Java 11